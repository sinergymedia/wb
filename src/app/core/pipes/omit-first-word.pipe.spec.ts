import { OmitFirstWordPipe } from './omit-first-word.pipe';

describe('OmitFirstWordPipe', () => {
  it('create an instance', () => {
    const pipe = new OmitFirstWordPipe();
    expect(pipe).toBeTruthy();
  });
});
